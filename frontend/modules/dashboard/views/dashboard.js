define([
  'text!frontendPath/dashboard/templates/dashboard.html',
  ],

  function(tpl) {
    return Backbone.Marionette.ItemView.extend({
      template: _.template(tpl),

      events: {
        
      },

      initialize: function() {
        
      },

      onRender: function() {
        
      },

      onShow: function() {
        
      },

    });
  });
