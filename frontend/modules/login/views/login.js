define([
  'text!frontendPath/login/templates/login.html',
  ],

  function(tpl) {
    return Backbone.Marionette.ItemView.extend({
      template: _.template(tpl),
      model: new Backbone.Model(),

      events: {
        
      },

      initialize: function() {
        
      },

      onRender: function() {
        
      },

      onShow: function() {
        
      },

    });
  });
